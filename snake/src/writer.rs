use ggez::{
    graphics::{ self, Text, Color },
}

// This entity will be in charge of managing
// all the text writing onto the screen
pub struct Write {}

impl Write {
  pub fn write(&mut self) -> Result<()> {

  }
}